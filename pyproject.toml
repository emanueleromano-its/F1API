[tool.poetry]
name = "f1api"
version = "0.1.0"
description = "Statistiche Formula 1 via F1Open API (scaffold)."

[tool.poetry.dependencies]
python = ">=3.7"
flask = "*"
requests = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
